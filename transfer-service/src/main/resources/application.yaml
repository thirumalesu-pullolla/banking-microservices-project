
server:
  port: 8084

spring:

  application:
    name: transfer-service

  datasource:
    url: jdbc:mysql://localhost:3306/transfer_db
    username: root
    password: Mysql@234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
