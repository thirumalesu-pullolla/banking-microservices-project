server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/transfer_db
    username: root
    password: Mysql@234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
